# LiftLink Root Gemfile
# This redirects to the React Native app

source "https://rubygems.org"

# The actual React Native project is in react-native-app/
# For Fastlane builds, cd into that directory first

gem "fastlane"
